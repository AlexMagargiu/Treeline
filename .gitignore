node_modules/
dist/
.next/
next-env.d.ts
*.tsbuildinfo

.env
.env.*
!.env.example

# Credentials for agents working on this project. See CLAUDE.md, Servers.
.secrets/

# A personal port mapping for a machine that already has something on 5432.
# Never committed, never copied to the box.
docker-compose.override.yml

# Coding prompts are working files. They are written for one agent, then deleted
# once the work is reviewed. See SUPERVISOR_PROMPT.md.
CODING_PROMPT_*.md

backups/

# Tile builds. The repository holds infra/tiles/build.sh and its README; the files
# themselves live in the tiles volume and are shipped up by hand. See infra/tiles/README.md.
infra/tiles/out/
infra/tiles/work/
