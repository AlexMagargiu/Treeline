{
  "name": "treeline",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "echo 'No end-to-end test yet. The first one arrives with the API prompt.'"
  },
  "devDependencies": {
    "@prisma/client": "6.5.0",
    "prisma": "6.5.0",
    "typescript": "5.7.3"
  }
}
